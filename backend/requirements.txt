# SMART AQI backend + ML + data pipeline
# Python 3.11.  Install:  python -m pip install -r backend/requirements.txt
#
# NOTE (Windows): tensorflow must be importable before xgboost or its native
# runtime fails to load. backend/ml/__init__.py enforces this order.

# --- web / api ---
Flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.0.1

# --- database ---
pymongo==4.7.2

# --- auth (Phase 12) ---
bcrypt==4.2.1
PyJWT==2.9.0            # not yet installed - added in the auth phase

# --- data / ml (installed) ---
pandas==2.2.2
numpy==1.26.4
scipy==1.17.1
scikit-learn==1.4.2
joblib==1.5.3
pyarrow==25.0.1
xgboost==3.2.0
tensorflow==2.21.0     # pulls keras==3.15.1

# --- pipelines (ingest / boundaries / spatial join) ---
requests==2.33.1
shapely==2.0.6         # station -> district point-in-polygon

# --- tests ---
pytest==8.3.4
